These three modules are kept in sync with the **`%%writefile`** cells in `../Test_II_Agentic_AI.ipynb`. If you edit the logic, update either the notebook cells or these files, then copy to the other side so they don’t drift.

Run agents/tests from the directory that contains `models.py` (e.g. after the notebook has written them to Colab’s cwd, or `cd` here and ensure DeepLenseSim is on `PYTHONPATH`).
